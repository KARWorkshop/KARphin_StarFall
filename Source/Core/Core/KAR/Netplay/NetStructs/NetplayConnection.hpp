#pragma once

//defines a netplay connection, used on both the server and client for sending packets of data

#include <Common/TraversalClient.h>

namespace Netplay::Core
{
	//defines a netplay connection
	//this is used to send packets 
	struct NetplayConnection
	{
		//establishes a connect to a server

		//sends a packet
	};
}