#pragma once

//defines general match settings

#include "Common/Config/ConfigInfo.h"
#include "Common/Config/Enums.h"
#include "Common/Config/Config.h"

namespace KAR::Game::Match
{
	//is high item frequency in CT enabled

	//are kirbies invinsable when not on a machine

	//is team mode enabled, where same colors can't hurt each other

	//can metaknigt and dedede be used in CT and die/respawn

	//what is the default start time of the match
}