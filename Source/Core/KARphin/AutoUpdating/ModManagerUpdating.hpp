#pragma once

// defines functions to let us auto update the mod manager

namespace KAR::AutoUpdate::ModManager
{
	//checks if the user has the mod manager installed

// checks if the mod manager needs a update

// performs a update of the mod manager

}  // namespace KARphin::AutoUpdate